# 2026-07-18

First day operating under hub (統合リナ) command via chat-relay. Three ordered
tasks — preserve uncommitted work, wire the conformance linter into CI, run the
A1 hooks→model codemod — plus a stage1 pre-stage token inventory that surfaced
an upstream codemod bug, and a docs honesty fix. All merges were self-merged on
green CI per hub's per-task authorization.

---

## Working-tree preservation (branch `docs/148-readme-unify`)

- Found six modified + eight untracked frontend files from a previous session: a
  brand refresh in progress (favicon set, `brand/` SVGs, IconLogo→IconBeacon
  swap, theme-color change). Committed honestly as WIP `4029c6e` and pushed.
  **Not merged** — hub ruled the branch stays parked: aesthetic territory,
  `npm run check` not run, provenance not in the hub ledger (owner to confirm).
- Handover noted: `main` later moved `serve.css` to `frontend/src/shared/ui/theme/`,
  so resuming this branch requires a rebase across that rename (measured).

## Conformance linter wired into CI (#163 → PR #164, merged `0b3a60f`)

- Gap (relayed from hub's fleet audit, then verified here): `composer conformance`
  existed since #144/#145 and sits in the `composer check` aggregate, but
  `ci.yml` calls individual scripts directly, so conformance had never run in CI.
- Fix: one step added to the `build` job between PHPStan and PHPUnit. Job names
  unchanged (`build`/`frontend` are required checks; branch protection locked
  with `enforce_admins` — relayed).
- Verified in the actual CI log before merging: `Conformance OK — no findings
  (0 suppressed by baseline/ignore)` (measured, run 29596105580).

## A1 hooks→model codemod (#165 → PR #166, merged `4a6a052`)

- One-shot run of `nene2-a1-hooks-to-model` (@hideyukimori/nene2-standards@1.1.0):
  11 moves (10 page hooks + `use-login-page.test.ts`) from `features/*/hooks/`
  to `features/*/model/`, 11 barrel/import rewrites, zero hand edits (M-1 purity).
- Local gate before push: `tsc -b` clean, 36 tests passed, eslint clean at
  `--max-warnings 0` (all measured). CI green on both jobs.
- Post-merge check against the codemod gap profile-rina reported (non-`use-*`
  files left behind in `hooks/` break relative imports): serve unaffected —
  `find` shows zero remaining `hooks/` directories and a forced full rebuild
  (`tsc -b --force`) is clean on merged `main` (measured).

## stage1 pre-stage: token inventory and mapping cross-check (no changes applied)

- Inventory (measured): 135 custom-property definitions, all in
  `frontend/src/shared/ui/theme/serve.css` — 51 unique theme tokens (51 light +
  42 dark definitions) plus 7 component-local names (42 definitions). All colors
  OKLCH. The 135 matches hub's own count (relayed, independently confirmed).
- Cross-check against nene2-tokens codemod-map v1.0.2 + contract v1 (read-only):
  17 names are already contract vocabulary, 32 are legal `x-` extensions,
  leaving **only 2 requiring rename** (`--space-x-nav-w`, `--space-x-topbar-h`).
  No serve entries exist (or are needed) in the mapping tables.
- Found an upstream bug by desk-checking the classifier: `space` is not a
  Tailwind v4 namespace, so the step-6 fallback re-inserts `x-` and would mangle
  the two names into `--space-x-x-*` on first run; the same family breaks
  suite's already-transformed `--r-x-base` on re-run (non-idempotent). Filed as
  **fleet-tooling#88** with both cases; hub ruled serve's remedy is a manual
  pre-rename to `--spacing-x-*` inside stage1 (no individual table).
- Hub rulings recorded for the wave: no contract-name declarations in stage1
  (15 of 32 contract names are undeclared here — themegen fill territory, W6);
  `x-st-*` status families stay as `x-` (contract-v2 second-wave ledger);
  stage1 for serve is var-rename only, utility-class translation N/A.

## CLAUDE.md honesty fix (#167 → PR #168, merged `e18a9f1`)

- The pre-stage inventory showed serve has no Tailwind at all (nothing in
  `package-lock.json`, no `@theme` — measured), while CLAUDE.md listed
  "Tailwind v4" in the sibling convention. Reworded to state the reality:
  hand-written design system v3 (`serve.css`, OKLCH tokens), Tailwind v4
  adoption planned for stage2 (W6). `docs/todo/current.md:68` left untouched —
  it is a historical record of a completed task.

## Remaining / handover

- serve stage1 after tokens publish: rename 2 vars + confirm codemod no-op.
- WIP brand-refresh branch awaits owner provenance confirmation (hub ledger),
  then rebase across the `serve.css` move.
- Daily-report home (`docs/daily/`) created today (#169); the Japanese-default
  vs ADR 0008 English-binding conflict is the same family as deal/vault
  (convention §7-1) and awaits the fleet-level exception ruling.

## 📊 Numbers

- Backend CI: `Conformance OK — no findings` first-ever CI execution (measured).
- Frontend gate on #166: 36 tests passed, eslint 0 warnings (measured).
- Token inventory: 135 definitions / 51 unique theme tokens / 2 renames needed
  (measured; 135 independently matches hub's count).
- Merged today: PR #164 (`0b3a60f`), PR #166 (`4a6a052`), PR #168 (`e18a9f1`).
  Not merged: WIP `4029c6e` (parked by ruling).

---

## Session 2

Three ordered tasks from hub, all completed: the fleet daily-report convention
v2 read-ack plus its D2 content-security self-check, W1 stage1 completion
(spacing pre-rename + official codemod acceptance), and the T0 front-test
inventory (read-only).

### Convention v2 ack + D2 self-check (#171 → PR #172, merged `e955aff`)

- Read the fleet daily-report convention v2; its new content-security section
  applies from today. This repo continues under the exception-candidate ruling:
  fleet format, English body (ADR 0008).
- Self-check of July dailies: the daily itself was clean on all five NG
  categories (measured, full-text pass); the one finding was an absolute
  internal workspace path in this folder's README boundary note, generalized in
  a 1-line diff. Both judgment calls (the path reading, and leaving the
  convention-sanctioned bare code-span references) were confirmed by hub.

### W1 stage1 completed (#173 → PR #174, merged `3079cdf`; tracking #175)

- Manual pre-rename `--space-x-{nav-w,topbar-h}` → `--spacing-x-*` (6
  occurrences in `serve.css`, values unchanged, `:root` retained) plus the two
  MAP targets in the bundled stage1 script — the fleet-tooling#88 avoidance
  hub ruled for serve.
- Acceptance: official `nene2-tokens@1.1.0` codemod is a **no-op** on the tree —
  plan empty, write-mode 0 replacements over `src/` (measured). Non-vacuous:
  the same invocation planned exactly the 2 predicted manglings before the fix
  (2 → 0, measured). `npm run check` green (36 tests, eslint 0 warnings,
  measured).
- With #155 (rename body) + #174 (fix + acceptance), serve stage1 matches the
  suite completion form — second in the fleet (hub confirmation, relayed).
- Deferred to stage2 and recorded in #175: dedicated theme-file split with
  direct `--theme` re-acceptance (the monolithic `serve.css` is rejected by the
  token-only theme grammar), and the bundled script's retirement conditions.

### T0 front-test inventory (read-only, report relayed to hub)

- `npm test` green: 20 files / 36 tests / 3.5s (measured). CI: the `frontend`
  job runs the full check (tests included) and is a required context together
  with `build`, `enforce_admins` on (measured via API).
- Coverage measured with a temporary `--no-save` provider: statements 67.68% /
  branches 62.74% / functions 62.30% / lines 68.13%. Caveat (measured): with
  `coverage.all` unset the denominator is only the ~65 test-imported files of
  168 source files, so the true whole-tree figure is far lower — flagged to
  hub as a ratchet-baseline precondition.
- Untested-logic TOP5 proposed for T1 (all in the A1-stable layer):
  auth-store/use-auth-token (0%), API error/pagination helpers,
  problem-details i18n mapping, the four untested entity mapper/mutation sets,
  and 9/10 page-model hooks (review and set-password first).
- No E2E/Playwright; MSW mock-first harness present; no front-specific
  security docs or explicit security tests (two auth-adjacent assertions only).

### T1-lite lane: the whole T0 TOP5 cleared (#179 · #181 · #183 · #185 · #187 · #189)

Hub opened a T1-lite lane on the T0 findings (test additions only, no
config/dependency changes; first PR pre-reviewed, then self-merge on green).
All five TOP5 items landed the same evening, in priority order:

- **#179** auth-store + use-auth-token (the 0%-coverage auth core): 10 tests —
  store/read/clear contract, sessionStorage persistence with a
  never-localStorage guard, subscription, hook reactivity. Hub review added
  one clarifying comment on the guard's future-proofing.
- **#181** shared/api errors: 11 tests — AppError mapping, `isRetryable`
  boundaries in both directions, RFC 7807 parsing incl. non-JSON fallback.
  `pagination.ts` was ruled untestable (pure type declaration) rather than
  padded — fleet-standard honesty call, hub-confirmed.
- **#183** i18n: 16 tests — all six status→key mappings, 5xx/unknown fallback,
  catalog fallback and `{{param}}` interpolation semantics.
- **#185** the four untested entities: 7 tests — advertiser/settings mappers
  (incl. the `configured ?? false` default) and asset/invitation mutations at
  the MSW boundary (wire-body capture, typed AppError rejections).
- **#187** the two hub-prioritized page hooks: 9 tests — review (four-eyes
  forbidden mapping) and set-password (token preview states, weak-password
  rejection).
- **#189** the remaining seven page hooks: 18 tests — load/error contracts,
  users invite flow, smtp save/test flows, and both directions of the home
  onboarding derivation. Also reworked the #187 queue assertion to the
  hub-ruled fleet standard (count + contract property + boundary anchor — no
  fixture-id transcription).

### 📊 Session 2 numbers

- Merged: PR #172 (`e955aff`), PR #174 (`3079cdf`), and the T1-lite lane
  #179/#181/#183/#185/#187/#189 — all CI green before merge (measured).
  Opened for tracking: #175, #176.
- Codemod acceptance: plan 2 renames before fix → 0 after; write mode 0
  replacements (measured).
- Test suite: **36 → 107 tests / 20 → 38 files** over the day (measured);
  coverage 4-tuple above (measured, caveat noted).
